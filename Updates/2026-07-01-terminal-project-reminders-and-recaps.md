# 2026-07-01 — Terminal project reminders + quick recaps

## What I did

- **Every Claude window now shows which project it's on — in two always-visible places:**
  - A small line pinned at the **bottom** of every Claude session: `⬢ You are working on: <Project> (branch)`.
  - The project name pinned in the **title bar** of each window (e.g. `⬢ Content Generation Platform`), so you can tell windows apart in Mission Control / the window switcher.
  - Both apply automatically to **every** project you open with your launcher — nothing to set up per project.
- **Claude will now give you a tiny recap after every change it makes**, in every project. It's three short bullets: what changed, the next step, and — importantly — a **"Your turn"** line that spells out anything *you* need to do (run a command, restart the app, etc.), or says "nothing".
- Left your fuller `Updates/` summaries and the `owner-update` habit exactly as they were — the new recap is the quick pulse, not a replacement.

## What I recommend next

- **Open a fresh window from your launcher to see it live** — the bottom status line only appears in *new* Claude sessions, not ones already running.
- **Heads-up (not urgent):** your `.claude/settings.local.json` has a GitHub token saved in plain text. It's normally kept private, but if it ever leaked I'd rotate it. Say the word and I'll help.
- If you want, I can add more to the reminder later — e.g. a color per project, the folder path, or a short custom tagline you pick for each one. Tell me what would help you most.
